{
  "name": "@ridhozhr10/config",
  "version": "1.0.0",
  "description": "Shared configuration package for loading environment variables.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "rimraf": "~6.0.0"
  }
}